# --- Required ---
# Your Hedera TESTNET account that receives the per-request HBAR payments.
SELLER_ACCOUNT_ID=0.0.123456

# Anthropic API key for the vibecode AI. The server returns 502 (after payment)
# if this is missing — set it before going live.
ANTHROPIC_API_KEY=
# Optional model override (default: claude-sonnet-4-5-20250929)
ANTHROPIC_MODEL=

# --- Buyer demo (live mode only; NOT needed for --dry-run) ---
# Buyer Hedera TESTNET account + ECDSA (secp256k1) private key.
# ED25519 keys do NOT work with the x402 tooling.
BUYER_ACCOUNT_ID=0.0.654321
BUYER_PRIVATE_KEY=

# --- Optional ---
# Port the server listens on (default 3000)
PORT=3000
# Public base URL of this server, used as the x402 resource URL in the 402.
# Default: http://localhost:PORT
PUBLIC_URL=
# Price per request in tinybars (default 5000000 ~= $0.01 at ~$0.20/HBAR)
PRICE_TINYBARS=5000000
# Base URL of the server for the buyer demo (default http://localhost:3000)
VIBECODE_URL=http://localhost:3000

# --- HCS audit feed (optional; audit logging is skipped if unset) ---
# Topic id from `npm run audit:init`
HCS_TOPIC_ID=0.0.789012
# Operator credentials used ONLY to submit audit messages (testnet ECDSA account)
AUDIT_OPERATOR_ID=0.0.123456
AUDIT_OPERATOR_KEY=

# --- Platform economics: 98/2 split (optional; skipped if TREASURY unset) ---
# Voicescape treasury account receiving 2% of every payment, forwarded
# on-chain by the operator after each x402 settlement.
TREASURY_ACCOUNT_ID=0.0.345678
# ECDSA key for SELLER_ACCOUNT_ID — used ONLY to forward the 2% treasury
# share. The operator keeps 98%.
SELLER_PRIVATE_KEY=
